{
  "name": "@h4designs/nuxt-variants",
  "version": "0.0.0",
  "description": "A centralized, deeply-merging configuration engine for Nuxt layouts with build-time variant graph and Nuxt Content v3 schema merging.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/happydesigns/nuxt-variants.git"
  },
  "files": [
    "dist"
  ],
  "type": "module",
  "main": "./dist/module.mjs",
  "typesVersions": {
    "*": {
      ".": [
        "./dist/types.d.mts"
      ]
    }
  },
  "exports": {
    ".": {
      "types": "./dist/types.d.mts",
      "import": "./dist/module.mjs"
    }
  },
  "scripts": {
    "prepack": "nuxt-module-build build",
    "prepare": "nuxt-module-build build --stub && nuxt-module-build prepare && nuxt prepare playground",
    "dev": "pnpm prepare && nuxt dev playground",
    "dev:build": "nuxt build playground",
    "release": "pnpm lint:check && pnpm typecheck && pnpm test && pnpm prepack && changelogen --release && npm publish && git push --follow-tags",
    "lint": "oxfmt && oxlint .",
    "lint:check": "oxfmt --check && oxlint .",
    "typecheck": "vue-tsc --noEmit && cd playground && vue-tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest watch",
    "test:types": "vue-tsc --noEmit && cd playground && vue-tsc --noEmit"
  },
  "dependencies": {
    "@nuxt/kit": "^4.4.2"
  },
  "devDependencies": {
    "@nuxt/devtools": "^3.2.4",
    "@nuxt/module-builder": "^1.0.2",
    "@nuxt/schema": "^4.4.2",
    "@nuxt/test-utils": "^4.0.0",
    "@types/node": "latest",
    "changelogen": "^0.6.2",
    "nuxt": "^4.4.2",
    "oxfmt": "^0.44.0",
    "oxlint": "^1.59.0",
    "typescript": "~6.0.2",
    "valibot": "^1.3.1",
    "vitest": "^4.1.2",
    "vue-tsc": "^3.2.6",
    "zod": "^4.3.6"
  }
}
