# Backlog

This backlog is intentionally lightweight. Move detailed implementation work
into task files under `.agents/tasks/ready/`.

## Now

- Verify the Docus documentation scaffold.
- Review whether the first docs pages cover the existing public API accurately.

## Next

- Plan any playground improvements as low-risk, isolated tasks.
- Consider an `ARCHITECTURE.md` for variant graph, runtime resolution, and schema merging boundaries.

## Later

- Evaluate module feature requests only after a high-level plan is approved.
- Add docs for migration/versioning once release workflows mature.

## Questions

- Which examples should be treated as canonical for future releases?
- Should the docs be deployed independently or as part of the package release workflow?
- Which checks are required before documentation changes can be considered complete?
